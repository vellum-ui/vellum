# @appjs/solid-renderer

Planned home for the SolidJS custom renderer targeting the AppJS runtime bridge.

## Goal

- Use Solid renderer primitives to map components to `appjs` widget ops.
- Keep this package independent from Rust internals.
